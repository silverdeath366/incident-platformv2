apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-backend.fullname" . }}
  labels:
    app: {{ include "incident-backend.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "incident-backend.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
